enum Direction{
    up,
    down,
    left,
    right
}

function doSoemthing(keyPressed:Direction)
{
    if(keyPressed==Direction.up) return
}
console.log(Direction.up);
console.log(Direction.down);
console.log(Direction.left);
